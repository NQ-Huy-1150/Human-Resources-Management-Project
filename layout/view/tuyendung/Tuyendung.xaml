<Page x:Class="layout.Tuyendung"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Tuyển dụng">
    <!-- VÙNG NỘI DUNG CHÍNH -->
    <Border Background="#F8F9FA">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Grid>
                <!-- Breadcrumb start -->
                <StackPanel Orientation="Horizontal" Margin="10,10,0,0" VerticalAlignment="Top">
                    <Button Content="Quản lý nhân sự" Height="25" Click="Button_Click" BorderThickness="0" Background="Transparent" FontStyle="Italic"/>
                    <TextBlock Text="/" Margin="5,4,5,0" FontSize="14" Foreground="Gray"/>
                    <TextBlock Text="Tuyển dụng" Margin="5,4,5,0" FontStyle="Italic" />
                </StackPanel>
                <!-- Breadcrumb end -->

                <Rectangle Height="2" Margin="10,40,10,0" Fill="LightGray" VerticalAlignment="Top"/>
                <Label Content="Tuyển dụng" HorizontalAlignment="Left" Height="37" Margin="10,51,0,0" VerticalAlignment="Top" Width="105" FontSize="18"/>
                
                <StackPanel Orientation="Vertical" Margin="10,90,10,10"  VerticalAlignment="Top" HorizontalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10" VerticalAlignment="Top" HorizontalAlignment="Right">
                        <Button Content="Tạo đơn Tuyển dụng" Margin="500,0,0,0" Click="CreateBtn"/>
                    </StackPanel>
                    <DataGrid x:Name="table" AutoGenerateColumns="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Mã đơn" Binding="{Binding id}" />
                            <DataGridTextColumn Header="Mã phòng ban" Binding="{Binding departmentId}" />
                            <DataGridTextColumn Header="Vị trí TD" Binding="{Binding position}" />
                            <DataGridTextColumn Header="Thời hạn" Binding="{Binding sub_deadline}" />
                            <DataGridTextColumn Header="Số Lượng" Binding="{Binding quantity}" />
                            <DataGridTextColumn Header="Trạng thái" Binding="{Binding status}" />
                            <DataGridTemplateColumn Header="Chức năng" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Xem" Click="btnDetail" Padding="5,1" Margin="10,0,0,0"/>
                                            <Button Content="Cập nhật" Click="updateBnt" Padding="5,1" Margin="10,0,0,0"/>
                                            <Button Content="Xóa" Click="deleteBnt" Padding="5,1" Margin="10,0,0,0"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Border>
</Page>
